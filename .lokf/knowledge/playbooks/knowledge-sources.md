---
type: Playbook
genre: reference
id: https://github.com/noelmcloughlin/ai-linkmo/knowledge/playbooks/knowledge-sources
title: Knowledge sources map
description: The librarian's scrape map - every repository location and external URL this bundle derives concepts from, and how to re-verify each on a refresh run.
resource: https://github.com/noelmcloughlin/ai-linkmo
generated:
  by: process:ktl-librarian
  at: "2026-09-25T20:33:14Z"
verified:
  - by: process:ktl-librarian
    at: "2026-09-25T20:33:14Z"
---

# Knowledge sources

Bootstrap discovery ran 2026-07-13; first steady-state refresh also ran 2026-07-13. Steady-state refresh runs re-verify these sources instead of re-discovering; update this map whenever the repository's knowledge geography changes.

| Source | Yields | Re-check by |
|--------|--------|-------------|
| `pyproject.toml`, root `README.md` | project identity, bundle metadata, install playbook | diff manifest name/description; re-read README sections. The README is a front door since 2026-09-25: its detail lives in `docs/`, rows below |
| `lib/cli/` (+ `./ai` wrapper, `lib/cli/README.md`) | `services/cli` | confirm `cli.py` still reads `lib/api/openapi.yaml`; fast/slow modes unchanged |
| `lib/api/` (`openapi.yaml`, `server*.py`, `handlers.py`, `lib/api/README.md`) | `services/fastapi-backend` | re-list `openapi.yaml` paths; confirm dynamic-generation architecture |
| `lib/frontend/` (`package.json`, `vite.config.ts`, `src/lib/constants.ts`, README) | `services/web-ui` | confirm Svelte 5 + Vite SPA, port 5173 proxy to :8000 |
| `graph/` (`ai-risk-ontology.yaml`, `cypher/`), `docs/neo4j.md`, `graph()` in `lib/api/handlers.py` | `services/graph-db`, `datasets/knowledge-graph-export` | confirm export commands and the Neo4j steps; confirm the Cypher path still ignores `--byod` (`export.py` loads packaged data only) |
| `graph/crosswalk_*.csv` | `datasets/nist-ai-rmf-crosswalks` | re-list `graph/*.csv`; header row = `fields` |
| `byo/data/*.yaml` (9 files) | one `datasets/` concept each | re-list the directory; each file's 3-line AUTOGENERATED header carries format + upstream source |
| `byo/data` file headers + in-file `url:` fields | the 11 `references/` concepts and their canonical URLs | grep `url:` in each data file; URLs must match the Reference's `resource` |
| `byo/olddata/` | mentioned in crosswalks concept only (inactive demo taxonomies) | consciously excluded - promote to a Dataset if reactivated |
| `byo/notes/EXAMPLES.md` | four-access-patterns framing used in service descriptions | re-read on refresh |
| README §Install + §Quick start, `docs/install-notes.md`, `docs/neo4j.md`, `lib/test/README.md`, `scripts/tests.sh` | `playbooks/install-and-run` | walk the commands against those pages; the concept's `sources` lists them |
| `docs/working-with-the-data.md` §Bring your own data + upstream ai-atlas-nexus util README | `playbooks/bring-your-own-data` | re-read the section; confirm the linkml validate incantation and the schema path under `ai_risk_ontology/schema/` |
| `docs/key-concepts.md` (moved out of the README 2026-09-25; grounded in the `openapi.yaml` entity endpoints and the `byod` parameter) | the eight `glossary/` terms | re-read the table; confirm each term still has a backing endpoint/flag and its example id still exists in the packaged data |
| README §Why governance needs a catalogue + §One model, four ways in (rationale, not mechanics) | `explanations/why-linked-ai-governance` | re-read both sections; the concept carries the *why*, so re-check the argument, not the feature list |
| README §Where this sits + `docs/for-the-curious.md` §Where this sits and §What it does not add yet | `explanations/what-ai-linkmo-adds` | re-read both; confirm the ontology still has no reviewer field and each `byo/data` file still carries one generator `dateCreated` |
| `docs/cli-examples.md`, `lib/test/test_cli_examples.py` | test facts in `services/cli` | recount the test cases; the page, not the README, now lists the commands |
| `https://ibm.github.io/ai-atlas-nexus/` | `references/ai-risk-ontology`, `org/ibm` | external authority - cite on change |
| `https://air-governance-framework.finos.org/` | `references/finos-aigf`, `org/finos` | external authority - cite on change |
| `.github/workflows/` (cli-tests, ui-tests) | test facts inside service concepts | re-list workflow files |
| `https://www.federalreserve.gov/supervisionreg/srletters/SR2602.htm` | the supersession note on `references/sr-11-7` | re-read until the curator settles its open question |

Not knowledge sources (consciously excluded): `lib/test/` internals, `byo/images/`, `.claude/`, `.agents/` and `skills-lock.json` (skill tooling, not project knowledge), `lib/frontend/STATUS.md` (transient dev notes), and the `.lokf/` sidecar itself including its automation workflows (`.github/workflows/knowledge-*.yaml`). Also excluded, re-decided 2026-09-25 after the README moved its detail into `docs/`: `docs/llm-inferencing.md` and the `/ares` endpoint (real, code-backed in `handlers.py`/`cli.py`, but covered generically by the OpenAPI-driven architecture in `services/cli` and `services/fastapi-backend`, so no per-endpoint concepts are minted); `docs/for-the-curious.md` §Curate your own open data, §Build Python applications on it, §Research (guidance that re-points to already-captured references); both roadmap sections, which name [asago-policy-mapper](https://github.com/asago-ai/asago-policy-mapper) and finos/fluxnova-ai#40 as possible future directions, not code-backed here (promote only once an integration lands); `docs/status.md` (caveats already carried where they bite: the Cypher/BYOD scope in the graph concepts, the unauthenticated writes in the install playbook, the reviewer gap in `explanations/what-ai-linkmo-adds`); `docs/releasing.md`, `docs/repository-layout.md`, `CHANGELOG.md`, `CONTRIBUTING.md`, `SECURITY.md`, `AI_COVENANT.md`, `CODE_OF_CONDUCT.md` (process and governance of the repository, not knowledge of the product); `NOTICE` (framework credits, already carried by the `references/` and `org/` concepts); and the README's §What it looks like examples (covered by `services/cli`).
