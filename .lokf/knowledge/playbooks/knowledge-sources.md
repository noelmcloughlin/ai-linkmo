---
type: Playbook
genre: reference
id: https://github.com/noelmcloughlin/ai-linkmo/knowledge/playbooks/knowledge-sources
title: Knowledge sources map
description: The librarian's scrape map - every repository location and external URL this bundle derives concepts from, and how to re-verify each on a refresh run.
resource: https://github.com/noelmcloughlin/ai-linkmo
timestamp: "2026-08-12T00:00:00Z"
---

# Knowledge sources

Bootstrap discovery ran 2026-07-13; first steady-state refresh also ran 2026-07-13. Steady-state refresh runs re-verify these sources instead of re-discovering; update this map whenever the repository's knowledge geography changes.

| Source | Yields | Re-check by |
|--------|--------|-------------|
| `pyproject.toml`, root `README.md` | project identity, bundle metadata, install playbook | diff manifest name/description; re-read README sections |
| `lib/cli/` (+ `./ai` wrapper, `lib/cli/README.md`) | `services/cli` | confirm `cli.py` still reads `lib/api/openapi.yaml`; fast/slow modes unchanged |
| `lib/api/` (`openapi.yaml`, `server*.py`, `handlers.py`, `lib/api/README.md`) | `services/fastapi-backend` | re-list `openapi.yaml` paths; confirm dynamic-generation architecture |
| `lib/frontend/` (`package.json`, `vite.config.ts`, `src/lib/constants.ts`, README) | `services/web-ui` | confirm Svelte 5 + Vite SPA, port 5173 proxy to :8000 |
| `graph/` (`ai-risk-ontology.yaml`, `cypher/`, README §Graph DB) | `services/graph-db`, `datasets/knowledge-graph-export` | confirm export commands and Neo4j import flow in README |
| `graph/crosswalk_*.csv` | `datasets/nist-ai-rmf-crosswalks` | re-list `graph/*.csv`; header row = `fields` |
| `byo/data/*.yaml` (9 files) | one `datasets/` concept each | re-list the directory; each file's 3-line AUTOGENERATED header carries format + upstream source |
| `byo/data` file headers + in-file `url:` fields | the 11 `references/` concepts and their canonical URLs | grep `url:` in each data file; URLs must match the Reference's `resource` |
| `byo/olddata/` | mentioned in crosswalks concept only (inactive demo taxonomies) | consciously excluded - promote to a Dataset if reactivated |
| `byo/notes/EXAMPLES.md` | four-access-patterns framing used in service descriptions | re-read on refresh |
| README §Quick Start (Install / Start the FastAPI Backend / Start the Web UI / Try the CLI) + §CLI/API Testing | `playbooks/install-and-run` | walk the commands against the current README |
| README §Working With the Data → §Bring Your Own Data + upstream ai-atlas-nexus util README | `playbooks/bring-your-own-data` | re-read the section; confirm the linkml validate incantation |
| README §What AI-LinkMO Does → §Key concepts, in plain English (grounded in the `openapi.yaml` entity endpoints and the `byod` parameter) | the six `glossary/` terms | re-read the table; confirm each term still has a backing endpoint/flag |
| README §The Problem + §What AI-LinkMO Does (rationale, not mechanics) | `explanations/why-linked-ai-governance` | re-read both sections; the concept carries the *why*, so re-check the argument, not the feature list |
| `https://ibm.github.io/ai-atlas-nexus/` | `references/ai-risk-ontology`, `org/ibm` | external authority - cite on change |
| `https://air-governance-framework.finos.org/` | `references/finos-aigf`, `org/finos` | external authority - cite on change |
| `.github/workflows/` (cli-tests, ui-tests) | test facts inside service concepts | re-list workflow files |

Not knowledge sources (consciously excluded): `lib/test/` internals, `byo/images/`, `scripts/tests.sh`, `.claude/`, `lib/frontend/STATUS.md` (transient dev notes), and the `.lokf/` sidecar itself including its automation workflows (`.github/workflows/knowledge-*.yaml`). Also excluded: README §LLM Inferencing and the `/ares` endpoint (real, code-backed in `handlers.py`/`cli.py`, but already covered generically by the dynamic OpenAPI-driven architecture in `services/cli` and `services/fastapi-backend` - no per-endpoint concepts are minted) and README §ARES Evaluation, §ADOPT?, §Curate your own Open Data, §Building Python Applications, §Research (narrative/aspirational content or guidance that re-points to already-captured references, not new facts). Partially promoted 2026-08-12 (Diátaxis facet added to LOKF 0.4.0): README §The Problem and §What AI-LinkMO Does were excluded on 2026-08-10 as "restating facts already captured", but understanding-oriented prose is now a first-class concept type, so their *rationale* is captured as `explanations/why-linked-ai-governance` and their §Key concepts glossary table as the six `glossary/` terms (both are table rows above). Still excluded from those sections: "What that looks like in practice" (CLI examples already covered by `services/cli`) and §About Trustworthy AI (a link list re-pointing at already-captured `references/*`). Also excluded: §Roadmap, which names [asago-policy-mapper](https://github.com/asago-ai/asago-policy-mapper) as a possible future upstream integration; it is not code-backed in this repository (no dependency, no integration point exists yet), so no `references/` concept is minted for it. Promote it to a real `references/` or `services/` concept only once an actual integration lands.
